#!/bin/bash
# Wrapper script for gVisor (runsc) with Podman conmon rootless execution
exec /usr/local/bin/runsc --ignore-cgroups {% if podman_gvisor_hostinet | default(false) %}--network=host {% endif %}"$@"
